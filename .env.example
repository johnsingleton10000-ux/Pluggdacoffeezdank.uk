# Public site URL only. Never place service-role or provider secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Add these when Supabase authentication is connected in a later stage.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
